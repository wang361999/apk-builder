{
  "name": "vercel-panel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev public --compatibility-date=2024-01-01",
    "deploy": "npx wrangler pages deploy public --project-name=vercel-panel"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "wrangler": "^3.60.0"
  }
}
